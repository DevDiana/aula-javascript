// Declare uma variavel de nome weight

let weight;

// Que tipo de dado é a variável acima?
console.log(typeof weight)

/*Declare uma variavel e atribua valores para cada um dos dados:
    *name: string
    *age: Number (integer(inteiro))
    *stars: Number (float)
    *isSubscribe: Boolean
*/
let name = "Diana"
let age = 25
let stars = 4.9
let isSubscribe = true

/*
    A variavel student abaixo é de que tipo de dado?
*/
let student = {
    name: "Diana",
    age: 23,
    stars: 4.9,
    isSubscribe: true,
    weight: 55
}

console.log(typeof student)

console.log(`${student.name} de idade ${student.age} com peso de ${student.weight}`)

